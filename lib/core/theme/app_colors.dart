import 'package:flutter/material.dart';

class AppColors {
  // Base Colors Light
  static const primary = Color(0xFF4CAF50);
  static const secondary = Color(0xFF81C784);
  static const accent = Color(0xFFFF5722);

  // Functional Colors Light
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFA726);
  static const error = Color(0xFFF44336);
  static const info = Color(0xFF2196F3);
  static const disabled = Color(0xFF9E9E9E);

  // Neutral Colors Light
  static const background = Color(0xFFFFFFFF);
  static const surface = Color(0xFFF5F5F5);
  static const border = Color(0xFFE0E0E0);
  static const divider = Color(0xFFEEEEEE);

  // Text Colors Light
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textDisabled = Color(0xFF9E9E9E);
  static const textInverse = Color(0xFFFFFFFF);

  // Base Colors Dark
  static const primaryDark = Color(0xFF66BB6A);
  static const secondaryDark = Color(0xFF98CF9B);
  static const accentDark = Color(0xFFFF7043);

  // Neutral Colors Dark
  static const backgroundDark = Color(0xFF121212);
  static const surfaceDark = Color(0xFF1E1E1E);
  static const surfaceLightDark = Color(0xFF2D2D2D);
  static const surfaceLighterDark = Color(0xFF333333);
  static const borderDark = Color(0xFF424242);
  static const dividerDark = Color(0xFF2D2D2D);
}
